# Production build (vite build) — empty API base => the dashboard calls the API
# with relative paths, so it talks to whichever origin served it (the gateway,
# which serves this SPA same-origin on :8080). Dev (`vite --host`) ignores this
# file and falls back to http://localhost:8080 (see src/api.ts).
VITE_API_BASE=
